/**
 * Polling configuration for real-time updates
 * All values in milliseconds
 */

export const POLLING_CONFIG = {
  // Stats dashboard (crawler statistics)
  STATS_INTERVAL: 10000, // 10 seconds

  // Jobs history (crawl jobs list)
  JOBS_INTERVAL: 10000, // 10 seconds

  // Job logs (when viewing logs during crawl)
  LOGS_INTERVAL: 10000, // 10 seconds

};
